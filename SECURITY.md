# Security policy

This repo documents fleet **roles and mechanisms only** — machine names, credentials, cloud identifiers, and private repository names are never allowed here. Run the redaction scan (`node scripts/redaction-scan.js .`) before opening a PR. A green check is not sufficient for merge: a human must read the scanner report (SOFT warnings included) before merging.
